# Code Conventions

> Extreme homogeneity. Agents predict better when the codebase looks like
> itself throughout. Fill in every TODO before an implementer starts work.

## Language/Style

- **Language & version:** TODO
- **Formatter/linter:** TODO (and the exact command to run it)
- **Line length / formatting rules:** TODO

## Names

TODO: a table of naming conventions per construct (modules, classes,
functions, constants, private members), with one example each.

## File Structure

TODO: the boilerplate/header every new file in this project should start
with, if any (license header, module docstring convention, import ordering).

## Tests

- **Test file location/naming:** TODO
- **Test framework:** TODO
- **Fixture/isolation convention:** TODO (e.g. "always use a temp dir, never
  touch real project files")

## Error Handling

TODO: this project's exception/error hierarchy or error-handling convention,
and how the user-facing layer is expected to surface failures.

## Comments

By default, comments are **not** written. They are only allowed when they
explain a non-obvious *why* (a documented workaround, a subtle invariant).
Well-named identifiers should do the rest.
